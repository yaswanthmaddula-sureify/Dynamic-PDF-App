

# How to start the app

- Install the node modules 

    ```javascript
     npm install
    ```
- Start the frontend vite app

    ```javascript
     npm run dev
    ```
- Start the nodejs server

    ```javascript
     npm run server
    ```
